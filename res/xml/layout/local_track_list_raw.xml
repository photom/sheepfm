<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:id="@+id/track_layout"
    android:layout_gravity="center_horizontal"
    >
    <ImageView android:id="@+id/track_toggle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_alignParentLeft="true"
        />

<TextView
    android:id="@+id/track_title"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:textSize="20sp"
    android:layout_centerVertical="true"
    android:textColor="@xml/track_txt_selector"
    android:layout_toLeftOf="@+id/track_playing"
	android:layout_toRightOf="@+id/track_toggle"
    />
<ImageView
    android:id="@+id/track_playing"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_centerVertical="true"

    android:layout_toLeftOf="@+id/track_cover"
    />
<ImageView
    android:id="@+id/track_cover"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_centerVertical="true"
    android:padding="4sp"
	android:layout_alignParentRight="true"
    />
</RelativeLayout>
